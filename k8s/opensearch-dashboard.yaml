apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboards

spec:
  replicas: 1

  selector:
    matchLabels:
      app: dashboards

  template:
    metadata:
      labels:
        app: dashboards

    spec:
      containers:
        - name: dashboards

          image: opensearchproject/opensearch-dashboards:2.19.0

          ports:
            - containerPort: 5601

          env:
            - name: OPENSEARCH_HOSTS
              value: '["http://opensearch:9200"]'

---
apiVersion: v1
kind: Service
metadata:
  name: dashboards

spec:
  selector:
    app: dashboards

  ports:
    - port: 5601
      targetPort: 5601

  type: ClusterIP